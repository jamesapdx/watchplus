"date +%N; date; echo 'This is a test'"
-x "free; echo; free -h; echo; top -b -n 1 -c -p $(pgrep -d',' -f watchplus)"
-b "dmesg" # best to just use dmesg, not dmesg -w (follow). avoid using tail, it's not needed, use -b
-s "ping 1.1.1.1"      # this is a good example of when to use the -s for streaming
-x "ping -c1 1.1.1.1"  # and don't use streaming here, because it outputs once and quits (-c1)
"python -c 'import timeit; print(\"{:f}\".format( timeit.default_timer() ) )'"
"cat /proc/interrupts"
"this is a junk line that will error"
"echo -e 'abcgxz abc \n123456\n7890 !@#$&^'"

# -s --streaming  For use with continual streaming commands such as tcpdump and ping.
# -b --bottom     Start this tab at the bottom of the output, similar to follow option.
# -x --change     Do not display change history. Can be toggled on/off with 'x'.
